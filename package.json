{
  "name": "blog",
  "version": "1.0.0",
  "description": "blog",
  "main": "n",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "GH=1 npm run build && bash scripts/deploy-gh.sh"
  },
  "author": "zhangting",
  "license": "ISC",
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^2.0.0-beta.49",
    "vuepress": "^2.0.0-beta.49"
  },
  "dependencies": {
    "vuepress-theme-hope": "^2.0.0-beta.87"
  }
}
