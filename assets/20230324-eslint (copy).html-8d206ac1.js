import{_ as n,W as s,X as a,a2 as e}from"./framework-609d4fec.js";const i={},t=e(`<h1 id="eslint" tabindex="-1"><a class="header-anchor" href="#eslint" aria-hidden="true">#</a> Eslint</h1><p>ESLint是一个用于识别和报告在ECMAScript/JavaScript代码中发现的模式的工具，其目标是使代码更加一致并避免错误。</p><p>ESlint完全是插件化的，每一条规则都是一个插件，可以在运行时添加更多插件。你也可以使用社区插件、配置和解析器来扩展ESlint的功能。</p><h2 id="预备" tabindex="-1"><a class="header-anchor" href="#预备" aria-hidden="true">#</a> 预备</h2><p>安装Nodejs（ (<code>^12.22.0</code>, <code>^14.17.0</code>, or <code>&gt;=16.0.0</code>)）并构建支持SSL。</p><h2 id="快速开始" tabindex="-1"><a class="header-anchor" href="#快速开始" aria-hidden="true">#</a> 快速开始</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">npm</span> init @eslint@config
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果想使用一个托管在npm上的共享配置，可以使用 <code>--config</code>选项后面加上配置包名称：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">npm</span> init @eslint/config -- <span class="token parameter variable">--config</span> shareablePackageName1, name2

<span class="token comment"># or (\`eslint-config\` prefix is optional)</span>
<span class="token function">npm</span> init @eslint/config -- <span class="token parameter variable">--config</span> eslint-config-semistandard
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>如果已经有 <code>package.json</code>，可以运行以下命令</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>npx eslint yourfile.js

<span class="token function">yarn</span> run eslint yourfile.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="配置" tabindex="-1"><a class="header-anchor" href="#配置" aria-hidden="true">#</a> 配置</h2><p>运行 <code>npm init @eslint/config</code> 之后，会生成一个 <code>.eslintrc.{js,yml,json}</code>文件。如下：</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token punctuation">{</span>
  <span class="token string-property property">&quot;rule&quot;</span><span class="token operator">:</span> <span class="token punctuation">{</span>
    <span class="token string-property property">&quot;semi&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&quot;error&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;always&quot;</span><span class="token punctuation">]</span><span class="token punctuation">,</span>
     <span class="token string-property property">&quot;quotes&quot;</span><span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&quot;error&quot;</span><span class="token punctuation">,</span> <span class="token string">&quot;double&quot;</span><span class="token punctuation">]</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>rule规则值的第一个值为错误等级，是以下值的一个</p><ul><li>off or 0 :关闭规则</li><li>warn or 1：警告，不会影响exit code</li><li>error or 2：错误，exit code为1</li></ul><div class="language-json line-numbers-mode" data-ext="json"><pre class="language-json"><code><span class="token punctuation">{</span>
    <span class="token property">&quot;extends&quot;</span><span class="token operator">:</span> <span class="token string">&quot;eslint:recommended&quot;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上面这个配置，会打开所有标志为推荐的规则。</p><p>可以在npm仓库中使用别人共享的配置。</p><p>ESlint不会检测你的代码，除非你从共享配置中扩展或者显式地在配置中打开规则。</p><h2 id="全局安装" tabindex="-1"><a class="header-anchor" href="#全局安装" aria-hidden="true">#</a> 全局安装</h2><p><code>npm install eslint --global</code></p><p>不推荐全局安装<code>eslint</code>，如果全局安装了 <code>eslint</code>，使用的任何插件或可共享配置任然需要在本地安装。</p><h2 id="手工调整" tabindex="-1"><a class="header-anchor" href="#手工调整" aria-hidden="true">#</a> 手工调整</h2><ol><li>安装ESlint</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">npm</span> <span class="token function">install</span> --save-dev eslint
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="2"><li>添加 <code>.eslintrc</code>文件</li><li>添加配置信息</li><li>Lint code using the ESLint CLI:</li></ol><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>npx eslint project-dir/ file1.js
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,28),l=[t];function o(c,d){return s(),a("div",null,l)}const p=n(i,[["render",o],["__file","20230324-eslint (copy).html.vue"]]);export{p as default};
